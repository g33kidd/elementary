var middleware = {

  /**
   * attach(new Server())
   * Attaches all middleware that is compatible to the server.
   */
  attach(server) {
    // loop this middleware/ dir and load into server by using server.use()
  }


}

export default { middleware }
